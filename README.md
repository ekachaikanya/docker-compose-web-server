
# 🌐 Project: Docker Compose Web Server (Week 06)

คู่มือฉบับสมบูรณ์สำหรับการสร้าง **Nginx Web Server** โดยใช้ **Docker Compose** พร้อมการตั้งค่า **Bind Mount** เพื่อเชื่อมโยงไฟล์หน้าเว็บจากเครื่อง Host

---

## 📁 1. โครงสร้างโปรเจกต์ (Project Structure)
ก่อนเริ่มต้น ให้สร้างโฟลเดอร์และไฟล์ดังนี้ในเครื่องของคุณ:
```text
my-web-project/
├── docker-compose.yaml       # ไฟล์ตั้งค่า Docker Compose
└── html/              # โฟลเดอร์สำหรับเก็บไฟล์หน้าเว็บ
    └── index.html     # ไฟล์หน้าเว็บหลัก
```

## 📄 2. ไฟล์หน้าเว็บ (index.html)
สร้างไฟล์ไว้ในโฟลเดอร์ html/ เพื่อใช้ทดสอบการแสดงผล:
```html
<!-- ไฟล์: html/index.html -->
<!DOCTYPE html>
<html>
<head>
    <title>My Docker Web Server</title>
</head>
<body>
    <h1>สวัสดีครับ! นี่คือหน้าเว็บของผมผ่าน Docker</h1>
    <p>แก้ไขไฟล์นี้ในเครื่อง (Host) แล้วกด Refresh ที่ Browser ได้เลย!</p>
</body>
</html>
```
## 🛠️ 3. การเขียนไฟล์ Docker Compose (compose.yaml)
สร้างไฟล์ชื่อ docker-compose.yaml (หรือ docker-compose.yml) โดยระบุการตั้งค่าดังนี้:

```yaml
name: my-web-project                      # ชื่อโปรเจกต์หลัก

services:
  my-web-server:                          # ชื่อ Service
    image: nginx:latest                   # ใช้ Image Nginx รุ่นล่าสุดจาก Docker Hub
    container_name: [username]-myweb      # ** หมายเหตุ: เปลี่ยน [username] เป็นชื่อของคุณ **
    ports:
      - "8080:80"                         # เข้าผ่านเครื่องเราที่ 8080 -> ส่งไปที่ Port 80 ของ Container
    volumes:
      # --- Bind Mount Configuration ---
      # [โฟลเดอร์ในเครื่องเรา] : [โฟลเดอร์ใน Container]
      - ./html:/usr/share/nginx/html      # เชื่อมโยงโฟลเดอร์งานเข้ากับตำแหน่งไฟล์ของ Nginx

```

## 🚀 4. คำสั่งควบคุมระบบ (Cheat Sheet)
เปิด Terminal หรือ Command Prompt ในโฟลเดอร์โปรเจกต์แล้วใช้คำสั่งดังนี้:
# เริ่มรันระบบ (Run)

```bash
# รันระบบในโหมด Background (เบื้องหลัง)
docker compose up -d
```

# หรือระบุชื่อ Service เจาะจงเพื่อรันตัวเดียว
docker compose up -d my-web-server
```
## การเข้าชมหน้าเว็บ: เปิด Browser ไปที่ http://localhost:8080

# หยุดการทำงาน (Stop & Remove)
```bash
# หยุดการทำงานและลบคอนเทนเนอร์ทิ้ง
docker compose down

```

## 💡 สรุปความรู้: Bind Mount
การใช้ volumes: แบบระบุ Path (เช่น ./html) เรียกว่า Bind Mount Docker Docs
* การทำงาน: เป็นการแชร์ไฟล์ระหว่างเครื่อง Host กับ Container โดยตรง
* ข้อดี: เมื่อคุณแก้ไขไฟล์ index.html ในเครื่องของคุณ ข้อมูลใน Container จะถูกอัปเดตทันที โดยไม่ต้องสั่ง Restart หรือ Build ใหม่ Docker Storage Overview
* ความแตกต่าง: ต่างจาก Named Volume ตรงที่เราสามารถจัดการไฟล์ผ่าน File Explorer ในเครื่องเราได้โดยตรง

# จัดทำขึ้นเพื่อการเรียนรู้วิธีการทำงานของ Docker Compose เท่านั้น
